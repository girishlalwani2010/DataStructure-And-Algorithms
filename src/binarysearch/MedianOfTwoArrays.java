package binarysearch;

/**
 * @author girish_lalwani
 *
 * Many variant problems on this 
 * 		Median of two sorted array of same size in log(min(m+n)) ideserve video
 * 		Median of two sorted array of different size in log(min(m+n))  tushar roy video
 *		Median of two un-sorted array of same/different size can be done using quicksort finding pivot till (m+n)/2
 *		Median of unsorted-stream can be done very efficiently using max and min heap, article reference explained very well
 *		http://www.ardendertat.com/2011/11/03/programming-interview-questions-13-median-of-integer-stream/
 */
public class MedianOfTwoArrays {
	
}
