package stack;

public class InfixOperationUsingStack {
	
	//(1+((2+5)*(4*5)))
	// answer should be 101
	
	// Two Stack Problem
	// Algorithm 
	// if left paranthesis comes ignore it.
	// if operand comes put it in operand stack.
	// if operator comes put it into operator Stack
	// if right paranthesis comes take two operands from stack, and one operator and compute and put result back in operand stack.
	// when the expression is completed fully operand stack element will be the answer
}
